import 'package:flutter/material.dart';


class MyToys extends StatelessWidget {
  const MyToys({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text("These are my toys")
    );
  }
}