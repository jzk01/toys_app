import 'package:flutter/material.dart';

class ToysList extends StatelessWidget {
  const ToysList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
     child: Text("This is the list")
    );
  }
}
